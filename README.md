
  # Portfolio Website Design

  This is a code bundle for my personal Design Portfolio Website .

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
